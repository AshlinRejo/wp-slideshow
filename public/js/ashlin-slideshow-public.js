( function( $ ) {
	'use strict';

	 $( function() {
		 $( '.as-slideshow' ).slideshowPlugin( {
			 // Parameters
			 effect: 'sliding',
			 slideSpeed: 800,
			 ratio: 'keep',
			 autoplay: 'true',
		 } );
	 } );

}( jQuery ) );
